# Workspace mapping
# HDMI-A-1 (LG ULTRAFINE) is strictly assigned to workspace 3
workspace = 3, monitor:HDMI-A-1, default:true

# Other workspaces are left unbound or assigned to other monitors as needed
# (Removing previous strict bindings to leave things "as is" per user request)
workspace = 1, monitor:eDP-1, default:true
workspace = 2, monitor:DP-1, default:true
